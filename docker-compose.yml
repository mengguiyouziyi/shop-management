version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - app-network

  # 可以在这里添加其他服务，如数据库等

networks:
  app-network:
    driver: bridge